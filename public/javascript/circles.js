/*
var ul = document.querySelector("main ul");
var li = ul.querySelectorAll("li");
li = Array.prototype.slice.call(li);
var len = li.length;
var step = 360 / len;
var angle = 0;
li.forEach(function(li, idx) {
  li.style.transform = "rotate(" + angle + "deg) translate(0,0)";
  angle += step;
});
*/
